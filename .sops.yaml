keys:
  # Age public key from ~/.config/sops/age/keys.txt on ortho-mac.
  # Generate on mac: age-keygen -y ~/.config/sops/age/keys.txt
  # Then replace the placeholder below and re-run: sops secrets/mac.yaml
  - &ortho_mac age13ymvhuqjfq8sjpz47kgvfldm6zlg9twckgskje8ghw2fmx95qykscwx7hx

creation_rules:
  - path_regex: secrets/.*\.ya?ml$
    key_groups:
      - age:
          - *ortho_mac
